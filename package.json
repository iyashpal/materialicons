{
  "name": "materialicons",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Yash Pal <iyashpal.dev@gmail.com>",
  "files": [
    "outline/",
    "round/",
    "solid/",
    "sharp/",
    "twotone/",
    "README.md"
  ],
  "scripts": {
    "build": "npm run build:vue && npm run build:react",
    "build:vue": "npm run script vue filled && npm run script vue outline && npm run script vue round && npm run script vue sharp && npm run script vue twotone",
    "build:react": "npm run script react filled && npm run script react outline && npm run script react round && npm run script react sharp && npm run script react twotone",
    "script": "node ./scripts/build.js"
  },
  "devDependencies": {
    "@babel/core": "^7.16.0",
    "@babel/plugin-transform-react-jsx": "^7.16.0",
    "@svgr/core": "^5.5.0",
    "@vue/compiler-dom": "^3.2.23",
    "camelcase": "^6.2.1",
    "rimraf": "^3.0.2",
    "svgo": "^2.8.0"
  }
}
